---
{
    "stream": {
        "$id": "sites-bottoms",
        "source": "knowledgeGraph",
        "destination": "pages",
      "fields": [
        "id",
        "uid",
        "name",
        "description",
        "photoGallery",
        "slug",
        "price",
        "color"
      ],
      "filter": {
        "entityTypes": [
          "products"
        ],
        "savedFilterIds": [
            "525581852"
        ]
      }
    },
    "urlFormat": "{{slug}}"
}
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Bottoms</title>
    <meta name="description" content=Browse through our selection of pants and shorts.>
    {{>head}}
    {{!-- <link href="https://unpkg.com/tailwindcss/dist/tailwind.min.css" rel="stylesheet"> --}}
    <!--Replace with your tailwind.css once created-->
    <link href="https://fonts.googleapis.com/css?family=Work+Sans:200,400&display=swap" rel="stylesheet">

  {{>styles}}

</head>

    <body>
        
    {{>navbar}}

    {{#each this}}
        <section class="bg-white py-8">
            <div class="grid grid-cols-5 gap-3">
                <div class="col-span-3">
                    <div class="w-full p-6 flex flex-col space-y-6">
                        {{>photoGallery photoGallery}}
                    </div>
                </div>

                <div class="col-span-2">
                    <div class="container py-8 px-6 mx-auto">
                        <div class="uppercase tracking-wide no-underline hover:no-underline font-bold text-gray-800 text-4xl mb-8">
                            {{name}} 
                        </div>
                        <p class="mt-8 mb-8 text-xl">{{description}}</p>
                        <p class="mt-8 text-xl"><b>Price:</b> ${{price}}</p>
                        <p class="mt-8 text-xl"><b>Color:</b> {{color}}</p>
                    </div>
                </div>
            </div>
        </section>
    {{/each}}

    </body> 

{{>footer}}

</html>